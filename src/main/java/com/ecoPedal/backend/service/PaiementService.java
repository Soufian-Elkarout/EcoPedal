package com.ecoPedal.backend.service;import com.ecoPedal.backend.entity.Paiement;import java.util.List;public interface PaiementService {      //Créer et enregistrer le paiement de caution lors de la réservation    Paiement creerPaiementCaution(Long reservationId);    /**     * Calculer et traiter le remboursement à la fin de la location     * Retourne le paiement mis à jour avec le montant remboursé     */    Paiement traiterRemboursement(Long locationId);     // Obtenir un paiement par son ID    Paiement getPaiementById(Long paiementId);     // Historique des paiements d'un utilisateur    List<Paiement> getPaiementsByUtilisateur(Long utilisateurId);}