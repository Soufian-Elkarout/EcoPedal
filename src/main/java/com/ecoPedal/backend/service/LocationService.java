package com.ecoPedal.backend.service;import com.ecoPedal.backend.entity.Location;import java.util.List;public interface LocationService {     //Démarrer une location après scan du QR code    Location demarrerLocation(Long utilisateurId, String qrCode);     //Terminer une location    Location terminerLocation(Long locationId);     // Obtenir une location par ID    Location getLocationById(Long locationId);     // Obtenir toutes les locations d'un utilisateur    List<Location> getLocationsByUtilisateur(Long utilisateurId);      // Obtenir les locations en cours    List<Location> getLocationsEnCours();}