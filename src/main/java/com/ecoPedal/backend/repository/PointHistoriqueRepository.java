package com.ecoPedal.backend.repository;import com.ecoPedal.backend.entity.PointHistorique;import com.ecoPedal.backend.entity.enums.TypePoint;import org.springframework.data.jpa.repository.JpaRepository;import org.springframework.stereotype.Repository;import java.util.List;@Repositorypublic interface PointHistoriqueRepository extends JpaRepository<PointHistorique, Long> {    // Historique complet des points d'un utilisateur    List<PointHistorique> findByUtilisateurId(Long utilisateurId);    // Historique par type (GAIN ou DEPENSE)    List<PointHistorique> findByUtilisateurIdAndType(Long utilisateurId, TypePoint type);    List<PointHistorique> findByUtilisateurIdOrderByDateCreationDesc(Long utilisateurId);}