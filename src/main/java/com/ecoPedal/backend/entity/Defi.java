package com.ecoPedal.backend.entity;import com.ecoPedal.backend.entity.enums.StatutDefi;import jakarta.persistence.*;import lombok.*;import java.time.LocalDateTime;import java.util.ArrayList;import java.util.List;@Getter@Setter@NoArgsConstructor@AllArgsConstructor@Entity@Table(name = "defi")public class Defi {    @Id    @GeneratedValue(strategy = GenerationType.IDENTITY)    private Long id;    private String titre;    private String description;    private Integer pointsRecompense;    private LocalDateTime dateDebut;    private LocalDateTime dateFin;    private Integer objective; // en km par exemple    @Enumerated(EnumType.STRING)    private StatutDefi statut;  // ACTIF, TERMINE, ANNULE    @OneToMany(mappedBy = "defi")    private List<Participation> participationList = new ArrayList<>();    @ManyToOne    @JoinColumn(name = "id_administrateur", nullable = false)    private Administrateur administrateur;}